import: 
  - recipe/laravel.php

config:
  repository: 'git@github.com:fathur/election-bot.git'

hosts:
  ec2-108-137-1-118.ap-southeast-3.compute.amazonaws.com:
    remote_user: ubuntu
    deploy_path: '~/election-bot'

tasks:
  build:
    - run: uptime  

after:
  deploy:failed: deploy:unlock
